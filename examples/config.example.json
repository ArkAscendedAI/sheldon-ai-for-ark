{
  "_comment": "Minimal config — only these two sections are required. Everything else has sensible defaults.",

  "llm": {
    "provider": "openrouter",
    "api_key": "your-api-key-here"
  },

  "auth": {
    "shared_secret": "generated-by-sheldon-bridge-init"
  }
}
